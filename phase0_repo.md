# Phase 0: Repository initialisation & first commit

You already created an empty GitHub repo at `https://github.com/VedantJadhav701/vidrag.git`.  
Inside the project folder (should be named `vidrag`), run these commands exactly.

```bash
echo "# vidrag" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/VedantJadhav701/vidrag.git
git push -u origin main
```

Verification: check the repo on GitHub – you should see the README.md.

When done, proceed to phase1_setup.md.